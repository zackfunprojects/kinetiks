// Empty stub for the `server-only` module under vitest.
// The real package throws when imported in a Client Component; vitest's
// "node" environment is server-side, so the guard is unnecessary.
export {};
